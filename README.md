# 🛡️ Система обучения кибербезопасности

Современная веб-платформа для обучения сотрудников государственных учреждений основам кибербезопасности, противодействия фишингу и криптографической защиты информации.

## 🌟 Возможности

### 📚 Образовательные курсы
- **🎣 Противодействие фишингу** - 8 разделов с интерактивными примерами
- **🔐 Криптографическая защита** - 4 модуля по работе с СКЗИ
- **📱 Интерактивные демо** - реалистичные примеры фишинговых атак

### 🎯 Тестирование и сертификация
- **Автоматические тесты** с мгновенной проверкой результатов
- **PDF-сертификаты** - генерация официальных заключений о допуске к работе с СКЗИ
- **Поддержка двух СКЗИ**: Крипто Про CSP и ViPNet Client

### 📊 Мониторинг прогресса
- **Персональная статистика** - отслеживание результатов обучения
- **История попыток** - детальный разбор всех тестирований
- **Прогресс-трекинг** - визуализация завершенности курсов

### 🔐 Безопасность и соответствие
- **Строгая аутентификация** - только для email с доменом `.gov.ru`
- **Политика конфиденциальности** - полное соответствие 152-ФЗ
- **Согласие на обработку данных** - юридически корректные формы

## 🏗️ Архитектура

### Технологический стек
- **Frontend**: React 18, React Router, HTML2Canvas, jsPDF
- **Backend**: Django 4.2, Django REST Framework
- **База данных**: PostgreSQL
- **Веб-сервер**: Nginx
- **Контейнеризация**: Docker, Docker Compose

### Структура проекта
.
├── frontend/ # React приложение
│ ├── src/
│ │ ├── components/ # React компоненты
│ │ ├── data/ # JSON с вопросами тестов
│ │ └── assets/ # Изображения и статические файлы
├── backend/ # Django приложение
│ ├── api/ # Основное Django приложение
│ └── backend/ # Настройки проекта
└── nginx/ # Конфигурация веб-сервера

text

## 🚀 Быстрый старт

### Предварительные требования

#### Установка Docker и Docker Compose

**Для Windows:**
1. Скачайте [Docker Desktop](https://www.docker.com/products/docker-desktop/)
2. Запустите установщик и следуйте инструкциям
3. После установки запустите Docker Desktop

**Для Linux (Ubuntu/Debian):**
```bash
# Установка Docker
sudo apt update
sudo apt install docker.io
sudo systemctl start docker
sudo systemctl enable docker

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo apt install docker-compose-plugin
Для Linux (Astra):

bash
# Добавление GPG ключа Docker
curl -fsSL https://download.docker.com/linux/debian/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Добавление репозитория Docker
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/debian $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Запуск и включение Docker
sudo systemctl start docker
sudo systemctl enable docker

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Проверка установки
docker --version
docker-compose --version
Установка и запуск
Клонирование репозитория

bash
git clone <URL-репозитория>
cd phishing-course
Настройка переменных окружения

bash
# Создайте файл .env со следующим содержимым:
DEBUG=True
DJANGO_SECRET_KEY=ваш_уникальный_ключ_безопасности
ALLOWED_HOSTS=localhost,127.0.0.1,web,nginx

DB_NAME=myapp
DB_USER=myuser
DB_PASSWORD=mypassword
DB_HOST=db
DB_PORT=5432

REACT_APP_API_URL=http://localhost:8000/api
Запуск приложения

bash
# Сборка и запуск всех контейнеров
docker-compose up --build

# Или для запуска в фоновом режиме
docker-compose up -d --build
Проверка статуса

bash
# Просмотр статуса контейнеров
docker-compose ps

# Просмотр логов
docker-compose logs
docker-compose logs web
docker-compose logs nginx
Создание администратора

bash
docker-compose exec web python manage.py createsuperuser
Приложение будет доступно по адресу: http://localhost

📖 Использование
Для администраторов
Управление пользователями через Django Admin (/admin)

Просмотр статистики обучения

Мониторинг активности системы

Для пользователей
Регистрация - только с email @adygheya.gov.ru

Прохождение курсов - последовательное изучение материалов

Сдача тестов - минимальный порог 70% для сертификации

Получение сертификатов - автоматическая генерация PDF

Доступные маршруты
/ - Страница входа

/main - Главная страница с курсами

/phish-course - Курс по противодействию фишингу

/crypto-course - Курс по криптографической защите

/quiz - Тест по фишингу

/crypto-quiz - Тест по криптографии

/stats - Персональная статистика

/phishing-example - Демо фишинговой атаки

🛠 Управление приложением
Основные команды
bash
# Остановка приложения
docker-compose down

# Перезапуск приложения
docker-compose restart

# Просмотр логов конкретного сервиса
docker-compose logs web -f
docker-compose logs nginx -f

# Выполнение команд в контейнере
docker-compose exec web python manage.py migrate
Резервное копирование и восстановление
bash
# Создание резервной копии базы данных
docker-compose exec db pg_dump -U myuser myapp > backup.sql

# Восстановление из резервной копии
docker-compose exec -T db psql -U myuser myapp < backup.sql
🔧 Разработка
Локальная разработка
bash
# Запуск в режиме разработки
docker-compose -f docker-compose.yml up --build

# Режим оффлайн (без внешних зависимостей)
docker-compose -f docker-compose.offline.yml up --build