events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    upstream backend {
        server web:8000;
    }

    server {
        listen 80;
        server_name localhost;
        
        # Корень для React
        root /app/staticfiles/frontend;

        # Статические файлы Django (админка)
        location /static/ {
            alias /app/staticfiles/;
            expires 1y;
            add_header Cache-Control "public, immutable";
            autoindex off;
        }

        # Статические файлы React - ИСПРАВЛЕНО
        location /static/js/ {
            alias /app/staticfiles/frontend/static/js/;
            expires 1y;
            add_header Cache-Control "public, immutable";
            try_files $uri $uri/ =404;
        }

        location /static/css/ {
            alias /app/staticfiles/frontend/static/css/;
            expires 1y;
            add_header Cache-Control "public, immutable";
            try_files $uri $uri/ =404;
        }

        location /static/media/ {
            alias /app/staticfiles/frontend/static/media/;
            expires 1y;
            add_header Cache-Control "public, immutable";
            try_files $uri $uri/ =404;
        }

        # Медиа файлы Django
        location /media/ {
            alias /app/staticfiles/media/;
            expires 1y;
            add_header Cache-Control "public";
            try_files $uri $uri/ =404;
        }

        # API запросы перенаправляем на Django
        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Админка Django
        location /admin/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # React Router - все остальные запросы на index.html
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}