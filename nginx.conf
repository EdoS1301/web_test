events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    upstream backend {
        server web:8000;
    }

    server {
        listen 80;
        server_name localhost;
        root /app/staticfiles/frontend;

        # Статические файлы React
        location /static/ {
            alias /app/staticfiles/frontend/static/;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Медиа файлы (если есть)
        location /media/ {
            alias /app/staticfiles/frontend/media/;
            expires 1y;
            add_header Cache-Control "public";
        }

        # API запросы перенаправляем на Django
        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Админка Django
        location /admin/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # React Router - все остальные запросы на index.html
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}